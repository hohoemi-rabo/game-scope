# GameScope フェーズ 2 MVP 仕様書

## Gaming ROI（コスパ管理機能）

---

## 1. 概要

ユーザーが購入したゲームの「金額」と「プレイ時間」を記録し、**時間あたりのコスト（CPH: Cost Per Hour）**を算出・可視化する機能。

**コンセプト**: ゲームを「消費」ではなく「投資」として捉え直し、ユーザーの自己肯定感を高める。

---

## 2. 認証方式

| 項目         | 内容                                                               |
| ------------ | ------------------------------------------------------------------ |
| 認証基盤     | Supabase Auth                                                      |
| ログイン方法 | Google OAuth（GCP で OAuth 2.0 クライアント ID を発行）            |
| 既存機能     | ログインなしで引き続き利用可能（トップ、検索、ニュース、更新状況） |

### 必要な設定

1. GCP Console で OAuth 同意画面を設定
2. OAuth 2.0 クライアント ID を作成（Web アプリケーション）
3. Supabase Dashboard > Authentication > Providers > Google に Client ID / Secret を設定
4. リダイレクト URL: `https://<project-ref>.supabase.co/auth/v1/callback`

---

## 3. データベース設計

### 3.1 新規テーブル: `user_portfolios`

```sql
create table user_portfolios (
  id uuid default uuid_generate_v4() primary key,
  user_id uuid references auth.users not null,
  game_id uuid references games(id) not null,

  -- 投資データ
  purchase_price integer default 0,          -- 購入金額（円）
  play_time_minutes integer default 0,       -- プレイ時間（分）
  is_subscription boolean default false,     -- サブスク/無料フラグ

  -- ステータス管理
  status text check (status in ('playing', 'completed', 'dropped', 'backlog')) default 'backlog',

  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  updated_at timestamp with time zone default timezone('utc'::text, now()) not null,

  unique(user_id, game_id)
);

-- インデックス
create index idx_user_portfolios_user on user_portfolios(user_id);
create index idx_user_portfolios_game on user_portfolios(game_id);

-- RLS（Row Level Security）
alter table user_portfolios enable row level security;

create policy "Users can view own portfolios"
  on user_portfolios for select
  using (auth.uid() = user_id);

create policy "Users can insert own portfolios"
  on user_portfolios for insert
  with check (auth.uid() = user_id);

create policy "Users can update own portfolios"
  on user_portfolios for update
  using (auth.uid() = user_id);

create policy "Users can delete own portfolios"
  on user_portfolios for delete
  using (auth.uid() = user_id);
```

### 3.2 既存テーブル拡張: `games`

Top 60 以外のゲームも格納できるよう、ユーザーが検索・登録したゲームを追加保存する。

- `rawg_id` カラムのユニーク制約を確認
- 既存のカラム構成で対応可能

### 3.3 将来の拡張用カラム（MVP 後に追加）

```sql
-- Phase 2.1 で追加予定
user_score integer check (user_score between 0 and 100),  -- 自己採点
review_comment text                                        -- 一言メモ
```

---

## 4. MVP 機能範囲

### 4.1 含める機能 ✅

| 機能               | 詳細                                                       |
| ------------------ | ---------------------------------------------------------- |
| Google ログイン    | Supabase Auth 経由                                         |
| ゲーム検索・登録   | RAWG API で検索 → DB に保存 → ポートフォリオに追加         |
| 入力項目           | 購入金額、プレイ時間、サブスク/無料フラグ、ステータス      |
| CPH 計算・表示     | 各ゲームの時間あたりコストを表示                           |
| ダッシュボード     | サマリー（総投資額、総プレイ時間、平均 CPH）＋ゲームリスト |
| コスパランクバッジ | CPH に応じた 5 段階バッジ表示                              |

### 4.2 後回しにする機能 ⏸️

| 機能               | 理由                            |
| ------------------ | ------------------------------- |
| 自己採点・一言メモ | カラム追加で後から対応可能      |
| ソート機能         | リスト表示後に追加しやすい      |
| 公開プロフィール   | ソーシャル機能は Phase 3 で検討 |
| フォロー機能       | 同上                            |

---

## 5. ステータス定義

| ステータス  | 意味         | 用途                     |
| ----------- | ------------ | ------------------------ |
| `playing`   | 現在プレイ中 | アクティブなゲーム       |
| `completed` | クリア済み   | 達成感の可視化           |
| `dropped`   | 途中でやめた | 合わなかったゲームの記録 |
| `backlog`   | 積みゲー     | 未着手ゲームの管理       |

---

## 6. CPH（Cost Per Hour）計算ロジック

### 6.1 基本計算式

```typescript
function calculateCPH(
  purchasePrice: number,
  playTimeMinutes: number,
  isSubscription: boolean
): number | null {
  // サブスク/無料の場合
  if (isSubscription || purchasePrice === 0) {
    return 0;
  }

  // 未プレイの場合
  if (playTimeMinutes === 0) {
    return null; // 「未プレイ」として表示
  }

  // 通常計算
  const playTimeHours = playTimeMinutes / 60;
  return Math.round(purchasePrice / playTimeHours);
}
```

### 6.2 コスパランク（Tier）

| ランク         | 条件（CPH）       | カラー   | メッセージ     |
| -------------- | ----------------- | -------- | -------------- |
| 💎 God Tier    | 0 円 〜 50 円     | Cyan     | 実質無料       |
| 🥇 Gold Tier   | 51 円 〜 200 円   | Gold     | 超優良投資     |
| 🥈 Silver Tier | 201 円 〜 500 円  | Silver   | 映画館より安い |
| 🥉 Bronze Tier | 501 円 〜 1500 円 | Bronze   | 適正価格       |
| 💸 Luxury      | 1501 円〜         | Red/Pink | 贅沢な遊び     |

---

## 7. UI/UX 設計

### 7.1 ナビゲーション

**未ログイン時**

```
🏆高評価 | 🔍検索 | 📰ニュース | 🔄更新状況 | [ログイン]
```

**ログイン済み時**

```
🏆高評価 | 🔍検索 | 📰ニュース | 🔄更新状況 | [👤 ▼]
                                              ├─ 📊 ダッシュボード
                                              ├─ ⚙️ 設定
                                              └─ ログアウト
```

### 7.2 ダッシュボード（/dashboard）

```
┌─────────────────────────────────────────────────────────┐
│  📊 マイダッシュボード                    [+ ゲームを登録] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │ 💰 総投資額  │ │ ⏱️ 総プレイ  │ │ 📈 平均CPH  │       │
│  │   ¥45,800   │ │   234時間   │ │  ¥196/時間  │       │
│  │             │ │             │ │  🥇 Gold    │       │
│  └─────────────┘ └─────────────┘ └─────────────┘       │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  登録ゲーム一覧                                          │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [画像] Elden Ring                               │   │
│  │        💎 ¥32/時間 | 280時間 | ¥9,000          │   │
│  │        ステータス: ✅ Completed                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ [画像] Baldur's Gate 3                          │   │
│  │        🥇 ¥75/時間 | 120時間 | ¥9,000          │   │
│  │        ステータス: 🎮 Playing                    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 7.3 ゲーム登録モーダル

**Step 1: 登録方法を選択**

```
┌─────────────────────────────────────────┐
│  ＋ ゲームを登録                   [×]  │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────┐ ┌─────────────────┐│
│  │  🏆 人気ゲーム   │ │  🔍 検索して   ││
│  │    から選ぶ     │ │     探す       ││
│  │                 │ │                ││
│  │  Top60から      │ │  RAWG APIで    ││
│  │  すぐに選べる   │ │  自由に検索    ││
│  └─────────────────┘ └─────────────────┘│
│                                         │
└─────────────────────────────────────────┘
```

**Step 2a: 人気ゲームから選ぶ**

- DB の `games` テーブル（Top 60）をグリッド/リスト表示
- API 呼び出しなし、高速レスポンス
- クリックで詳細入力へ

```
┌─────────────────────────────────────────┐
│  🏆 人気ゲームから選ぶ         [← 戻る] │
├─────────────────────────────────────────┤
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐      │
│  │[img]│ │[img]│ │[img]│ │[img]│      │
│  │Elden│ │BG3  │ │Zelda│ │GTA V│      │
│  └─────┘ └─────┘ └─────┘ └─────┘      │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐      │
│  │[img]│ │[img]│ │[img]│ │[img]│      │
│  │FF16 │ │RE4  │ │Hades│ │...  │      │
│  └─────┘ └─────┘ └─────┘ └─────┘      │
│                                         │
│            [ もっと見る ]               │
└─────────────────────────────────────────┘
```

**Step 2b: 検索して探す**

- RAWG API で自由検索（500ms debounce）
- マイナーゲーム、インディーズも登録可能
- 選択時に `games` テーブルへ upsert

```
┌─────────────────────────────────────────┐
│  🔍 検索して探す               [← 戻る] │
├─────────────────────────────────────────┤
│  [検索ワードを入力...              🔍]  │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ [img] Hollow Knight (2017)     │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │ [img] Hollow Knight: Silksong  │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

**Step 3: 詳細入力**

```
┌─────────────────────────────────────────┐
│  📝 ゲームを登録                   [×]  │
├─────────────────────────────────────────┤
│  [img] Elden Ring                       │
│                                         │
│  購入金額                               │
│  [¥ 9000                           ]    │
│  □ サブスク/無料で入手                  │
│                                         │
│  プレイ時間                             │
│  [280        ] 時間                     │
│                                         │
│  ステータス                             │
│  [Completed ▼]                          │
│   ├─ 🎮 Playing（プレイ中）             │
│   ├─ ✅ Completed（クリア済み）         │
│   ├─ ❌ Dropped（やめた）               │
│   └─ 📚 Backlog（積みゲー）             │
│                                         │
│            [キャンセル] [登録する]       │
└─────────────────────────────────────────┘
```

---

## 8. 開発ステップ

### Phase 2.0（MVP）

| #   | タスク             | 詳細                                               |
| --- | ------------------ | -------------------------------------------------- |
| 1   | 認証セットアップ   | GCP OAuth 設定 → Supabase Auth 連携                |
| 2   | DB Migration       | `user_portfolios` テーブル作成、RLS 設定           |
| 3   | 認証 UI            | ログインボタン、ユーザーメニュー（ドロップダウン） |
| 4   | ゲーム検索 API     | RAWG API 検索 → games テーブルへの upsert          |
| 5   | ポートフォリオ登録 | Server Action: `createPortfolioEntry`              |
| 6   | ダッシュボード UI  | サマリー表示 + ゲームリスト + CPH バッジ           |
| 7   | 編集・削除機能     | 登録済みゲームの更新・削除                         |

### Phase 2.1（MVP 後の拡張）

- 自己採点・一言メモ機能
- ソート機能（CPH 順、プレイ時間順、登録日順）
- フィルター機能（ステータス別）

### Phase 3（将来構想）

- 公開プロフィール
- ユーザー間フォロー
- コスパランキング（全体/フレンド）

---

## 9. 技術スタック（追加分）

| 技術            | 用途                                 |
| --------------- | ------------------------------------ |
| Supabase Auth   | Google OAuth 認証                    |
| Supabase RLS    | ユーザーデータの行レベルセキュリティ |
| Server Actions  | ポートフォリオ CRUD 操作             |
| React Hook Form | フォーム管理（任意）                 |

---

## 10. 注意事項

1. **RAWG API 制限**: 無料プランは 20,000 リクエスト/月。検索時の debounce（500ms）を維持
2. **RLS 必須**: `user_portfolios` は必ず RLS を有効化し、他ユーザーのデータにアクセスできないようにする
3. **games テーブル拡張**: Top 60 以外のゲームが追加されることを考慮し、`is_top_rated` フラグなどで区別する案も検討
